import React from 'react'
import { render, cleanup } from '../../../test/testUtils'
import SkenarioPrioritasToolbar from '../SkenarioPrioritasToolbar'


describe('SkenarioPrioritasToolbar component', () => {
  it('SkenarioPrioritasToolbar component rendered', () => {
    const checkSkenarioPrioritasToolbar = render(<SkenarioPrioritasToolbar />, {})
  })
})

